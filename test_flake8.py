import fastapi
from fastapi import HTTPException
import os
import sys
import sys


def my_function(arg1, arg2):  # неправильное имя функции
    x = 10  # неправильное имя переменной
    y = 20
    if x > y:
        print("x is greater")
    else:
        print('y is greater or equal')  # строка длиннее 79 символов
    return x + y

a = fastapi.FastAPI()
def f2A(a):
    a = 2  # s
    os.close()  # as
    sys.stdin
    print(a)
    val = "g"
    print(val)


print(str)  # использование встроенной функции как переменной
